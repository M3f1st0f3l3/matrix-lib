This is a simple matrix library. As it is right now, it can be used to form matrices, fill them with a desired number and so on.

once the project is downloaded executes the following commands:

mkdir build
cd build
cmake ..
make
ctests 

The last one is used to run the test projects to ensure that everything is ok.